# 五分钟快速开始

1. 安装：`pip install -r requirements.txt`
2. Excel放到：`input/cases.xlsx`
3. PDF放到：`input/reports/`
4. 执行：`python main.py run-all`
5. 查看：`knowledge/standard_case/`

也可以直接指定真实路径：

```bash
python main.py run-all --excel "真实Excel路径" --reports-dir "真实PDF目录"
```
