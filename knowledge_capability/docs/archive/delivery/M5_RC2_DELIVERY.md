# REPEAT_CASE_ENGINE V2.3 M5 RC2

## 修复项

恢复候选案例 AI 分析的多线程执行能力。

## 实现范围

- M8.2 相似性分析支持候选案例并发执行。
- M8.3 解决方案分析支持候选案例并发执行。
- Pipeline 阶段顺序保持不变。
- 并发结果按输入候选顺序汇总。
- 单候选、关闭并发或 `skip_ai` 时自动串行执行。
- 单个候选失败不会中断其他候选任务。

## 配置

```yaml
parallel_ai:
  enabled: true
  max_workers: 4
```

## 测试

- 新增并发执行顺序、线程使用和串行降级测试。
- 全量回归：109 passed。
