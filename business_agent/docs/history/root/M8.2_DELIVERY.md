# REPEAT_CASE_ENGINE V2.0 M8.2 交付说明

## 本次交付

M8.2 Similarity Analyzer 已实现。输入 M8.1 生成的 Analysis Context，逐候选案例输出多维相似性分析。

新增：

- `builder/similarity_analyzer.py`
- `builder/m82_similarity_runner.py`
- `prompts/similarity_analyzer.md`
- `schema/similarity_analysis.schema.json`
- `tests/samples/mock_similarity_response.json`
- `tests/test_m82_similarity_analyzer.py`
- CLI：`run-m82-similarity`

## 分析维度

- 问题/故障对象
- 现象
- 触发条件
- 影响
- 失效机理
- TRC
- MRC
- 根因
- 原因分类
- 组织上下文

每个维度输出：评分、判断、双方证据、判断理由。整体输出综合评分、相似等级、关键相似点、关键差异、证据缺口、摘要和置信度。

## 运行

```bash
python main.py run-m82-similarity --query-id QUERY001 --overwrite
```

本地 Mock：

```bash
python main.py run-m82-similarity --query-id QUERY001 --mock --overwrite
```

跳过 AI：

```bash
python main.py run-m82-similarity --query-id QUERY001 --skip-ai --overwrite
```

单候选：

```bash
python main.py run-m82-similarity --query-id QUERY001 --case-id CASE-000001 --overwrite
```

输出：

```text
knowledge/similarity_analysis/{query_id}/{case_id}.json
output/logs/m82_similarity_summary.json
```

## 降级规则

AI 未启用、调用失败或输出不符合 Schema 时，不伪造相似性结论；输出 `UNKNOWN` 或失败状态，并保留 warning。
